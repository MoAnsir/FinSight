node_modules
dist
.next
.turbo
coverage
*.log
.DS_Store

# env files — never commit real secrets
.env
.env.*
!.env.example

# generated files — routeTree.gen.ts is committed so CI typecheck works without a build step
